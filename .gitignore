# Basic gitignore
/[Tt]emp/
*.exe

# Claude Code settings
.claude/settings.local.json
# macOS
.DS_Store

# Local credentials — never commit real API keys
.env

# Pre-rebuild audit scaffolding — not user documentation
AUDIT.md
